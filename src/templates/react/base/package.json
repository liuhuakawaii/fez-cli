{
  "name": "<%= projectName %>",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "<%= bundler === 'vite' ? 'vite' : 'webpack serve --mode development' %>",
    "build": "<%= bundler === 'vite' ? 'vite build' : 'webpack --mode production' %>",
    "preview": "<%= bundler === 'vite' ? 'vite preview' : 'serve dist' %>"
  }
}
